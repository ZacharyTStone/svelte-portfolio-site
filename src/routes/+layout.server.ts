/*During the build process, the application will generate static HTML files for the pages, 
which can improve the initial loading speed and SEO performance of the application.*/
export const prerender = true;
